{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <h1>Evenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evenement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ evenement.type }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ evenement.lieu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ evenement.etat }}</td>
            </tr>
            <tr>
                <th>Amount_earned</th>
                <td>{{ evenement.amountEarned }}</td>
            </tr >
        </tbody>
    </table>

    <a href="{{ path('evenement_index') }}" class="btn btn-info">back to list</a>
&nbsp;
    <a href="{{ path('evenement_edit', {'id': evenement.id}) }}" class="btn btn-warning">edit</a>
&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;

&nbsp;{{ include('evenement/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Sponsor index{% endblock %}

{% block body %}
    <h1>Sponsor index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomSponsor</th>
                <th>Motant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sponsor in sponsors %}
            <tr>
                <td>{{ sponsor.id }}</td>
                <td>{{ sponsor.nomSponsor }}</td>
                <td>{{ sponsor.motant }}</td>
                <td>
                    <a href="{{ path('sponsor_show', {'id': sponsor.id}) }}" class="btn btn-primary">show</a>
                    <a href="{{ path('sponsor_edit', {'id': sponsor.id}) }}" class="btn btn-warning">edit</a>
                    <a href="/sponsor/delete/{{ sponsor.id }}" class="btn btn-danger"   onclick="return confirm('Etes-vous sûr de supprimer cet article?');">Delete</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('sponsor_new') }}" class="btn btn-dark">Create new</a>
{% endblock %}
